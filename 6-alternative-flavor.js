//Different syntax but exactly the same with names and utils js to export module variables
module.exports.items = ["item1", "item2"]
const person = {
    name: "bob",
}

module.exports.singlePerson = person